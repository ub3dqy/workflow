{
  "mcpServers": {
    "workflow-mailbox": {
      "command": "workflow-mailbox-channel",
      "args": [
        "--project",
        "workflow",
        "--agent",
        "claude",
        "--interval-ms",
        "3000"
      ]
    }
  }
}
